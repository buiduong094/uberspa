import React from 'react';
import { ActivityIndicator, View } from 'react-native';

const LoadingSpine = () => (

  <ActivityIndicator
  color="red"
    size="large"
  />

);

export default LoadingSpine;
